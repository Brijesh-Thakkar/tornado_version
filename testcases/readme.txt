
The test cases were used by first of all exporting the file in the msc file format. Then exporting this file in various file formats given and opening and checking for each file whether it had successfully saved each of the components. Then these files were imported into the socialcalc editor and checked whether they got successfully uploaded (It was done in this way, since all the exports were taking place correctly and hence we needed to check for the import parts mainly)

But, the biggest issue of them all, the xlsx files created/changed in gnumeric tool does not get imported in the socialcalc. Whereas, files changed/created in MS office are getting imported correctly (Strange !!!).

The CSV file format does not provide for much formatting and therefore, didn't store the various formatting elements.

The things not mentioned in the issues.txt in each folder can be assumed to be verified.

-- test
