{
  "name": "tornado-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end Playwright tests for Tornado spreadsheet application",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:intercepta": "jest tests/interceptaScreening.test.js --runInBand"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "jest": "^30.5.2",
    "typescript": "^5.0.0"
  }
}
